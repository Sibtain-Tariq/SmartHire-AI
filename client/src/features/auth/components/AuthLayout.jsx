import React from 'react'

export default function AuthLayout({ children }) {
  return (
    <div className="auth-layout-placeholder">
      {/* Auth layout structure placeholder */}
      {children}
    </div>
  )
}
